use dac23;

Create table Faculty(
	faId int,
    faFirstName varchar(20),
    faLastName varchar(20),
    faAge varchar(10),
    faSpecilization varchar(50),
    faAddress varchar(50),
    faPhoneNo varchar(20)
);

desc Faculty;

select * from Faculty;